# Form Builder — Frontend Take-Home

Scaffolded with Vite + React + TypeScript + Tailwind CSS. Build in progress — see `docs/plan.md` for architecture and roadmap.

## Run locally

```bash
npm install
npm run dev
```

Other scripts: `npm run build`, `npm run test`, `npm run lint`.

_(Full README — localStorage schema, architectural decisions, trade-offs — lands in the final implementation step.)_
